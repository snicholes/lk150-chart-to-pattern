# lk150-chart-to-pattern
super easy page that lets you draw out a colorwork chart (e.g. fair isle) and will generate the corresponding pattern to use on a non-punchcard knitting machine (e.g. silver reed lk150).
